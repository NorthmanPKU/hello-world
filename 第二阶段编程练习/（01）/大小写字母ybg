输入一个字符，判断是大写字母还是小写字母，是大写字母输出"upper case",是小写字母输出"lower case",如果都不是则输出"invalid input".
关于输入
输入为一个字符
关于输出
输出为字符的信息
例子输入
s
例子输出
lower case
#include<iostream>
#include<stdlib.h>
#include<iomanip>
#include<cmath>
#include<math.h>

using namespace std;

int main()
{
    char a=0;
    cin>>a;
    if(a>=65&&a<=90)
        cout<<"upper case";
    else if(a>=97&&a<=122)
        cout<<"lower case";
    else
        cout<<"invalid input";
    
    return 0;
}
//YBG
